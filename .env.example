# Claude Telegram Bot - Environment Configuration

# ==============================================================================
# REQUIRED
# ==============================================================================

# Telegram bot token from @BotFather
TELEGRAM_BOT_TOKEN=1234567890:ABC-DEF1234ghIkl-zyx57W2v1u123ew11

# Comma-separated Telegram user IDs allowed to use the bot
# Find your ID: message @userinfobot on Telegram
TELEGRAM_ALLOWED_USERS=123456789

# ==============================================================================
# RECOMMENDED
# ==============================================================================

# Projects root directory (parent directory containing all your projects)
# Use /project command to switch between subdirectories at runtime
# Default: home directory
# Example: PROJECTS_ROOT=/Users/yourname/Dev
PROJECTS_ROOT=/Users/yourname/Dev

# DEPRECATED: OpenAI API key no longer required for voice transcription
# Voice transcription now uses on-device Apple 'hear' tool + translate-shell
# Keep this only if you need OpenAI for other features (not currently used)
# OPENAI_API_KEY=sk-...

# ==============================================================================
# OPTIONAL - Security
# ==============================================================================

# Comma-separated paths Claude can access
# Default: working dir, ~/Documents, ~/Downloads, ~/Desktop, ~/.claude
# Note: Setting this OVERRIDES defaults. Include ~/.claude for plan mode to work.
# ALLOWED_PATHS=/Users/yourname/personal,/Users/yourname/projects,/Users/yourname/.claude

# Rate limiting (token bucket)
# RATE_LIMIT_ENABLED=true
# RATE_LIMIT_REQUESTS=20
# RATE_LIMIT_WINDOW=60

# Permission mode: "bypass" (no prompts, default) or "interactive" (show dialogs)
# PERMISSION_MODE=bypass

# Allow changing permission mode via /permissions command (default: true)
# ALLOW_TELEGRAM_PERMISSIONS_MODE=true

# Model configuration
# MODEL_DEFAULT=sonnet                    # Default model: opus, sonnet, or haiku
# ALLOW_TELEGRAM_MODEL_MODE=true         # Allow switching models via /model command

# ==============================================================================
# OPTIONAL - Claude Authentication
# ==============================================================================

# API key for environments without Claude Code CLI auth
# Get from: https://console.anthropic.com/
# Note: API usage is billed per token - CLI auth is more cost-effective
# ANTHROPIC_API_KEY=sk-ant-api03-...

# Path to Claude CLI (auto-detected from PATH by default)
# CLAUDE_CLI_PATH=/usr/local/bin/claude

# ==============================================================================
# OPTIONAL - Extended Thinking
# ==============================================================================

# Default thinking level: 0 (off), 10000 (normal), 50000 (deep)
# Can be changed at runtime via /thinking command
# THINKING_DEFAULT=10000

# ==============================================================================
# OPTIONAL - Voice Transcription
# ==============================================================================

# Voice transcription uses Apple's on-device 'hear' tool + translate-shell
# Required tools (install if missing):
#   - ffmpeg: brew install ffmpeg
#   - hear: comes with macOS (Apple's speech recognition)
#   - translate-shell: brew install translate-shell
#
# Voice locale is configured via /voicelocale command in Telegram (default: en-US)
# For non-English locales, voice is automatically translated to English before sending to Claude
# Run 'hear -s' to see all supported locales

# Translation target language (default: en for English)
# Set this if you want translations in a different language
# VOICE_TRANSLATION_TARGET=en

# Additional context for voice transcription (names, technical terms, etc.)
# This may not be used by all transcription backends
# TRANSCRIPTION_CONTEXT=Common names: John, Alice. Tech: Kubernetes, GraphQL.

# ==============================================================================
# OPTIONAL - Logging
# ==============================================================================

# Audit log path (default: ~/.sickfar/logs/audit.log)
# AUDIT_LOG_PATH=/path/to/custom/audit.log

# Output audit logs as JSON (default: human-readable)
# AUDIT_LOG_JSON=false

# Audit log rotation settings
# AUDIT_LOG_MAX_SIZE_MB=10    # Rotate when log reaches this size (default: 10MB)
# AUDIT_LOG_MAX_FILES=5        # Keep this many rotated files (default: 5)
